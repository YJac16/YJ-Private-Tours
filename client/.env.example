# Optional: absolute booking API base (defaults to /api via Vite proxy in dev)
# VITE_BOOKING_API_URL=http://localhost:3000/api

# Yoco public key (safe to expose — used for display / future client SDK)
VITE_YOCO_PUBLIC_KEY=pk_test_226e7048lWA8MwPc8454
